#lang racket

;;; Exercise 2.66

; Nope. Give us an actual representation of the data. (Essentially this is just element-of? for sets using the binary tree representation, but slap an application of key onto the entry and return the found entry instead of #t.)
